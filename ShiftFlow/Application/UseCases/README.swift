// ShiftFlow — Application Layer
// UseCases/README.swift
//
// TASK-FOUNDATION-001: Directory placeholder.
//
// This directory will contain use case implementations that coordinate
// between the UI layer and the Domain layer.
//
// Examples of future use cases:
// - AssignShiftUseCase (assigns a shift to a date)
// - GetScheduleUseCase (retrieves resolved schedule for a date range)
// - ManageTasksUseCase (adds/removes tasks from a work day)
// - ManageNotesUseCase (adds/edits notes on a work day)
//
// Use cases depend on Domain layer services and persistence abstractions.
// They must NOT contain SwiftUI view logic.

import Foundation

/// Placeholder — Application layer use cases will be added in later tasks.
enum UseCasesPlaceholder {
    // Intentionally empty.
}
